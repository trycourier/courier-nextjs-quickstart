import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // This sample is the reference; it does not need generated agent rule files.
  agentRules: false,
};

export default nextConfig;
